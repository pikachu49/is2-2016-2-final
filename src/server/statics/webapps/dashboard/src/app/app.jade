div.container

    div.content-container
        router-outlet

    div.left-panel
        div(*ngFor="let module of modules")
            button.subtitle {{ module.name }}
            div.actions-container
                a(routerLink="{{ action.path }}", *ngFor="let action of module.actions")
                    button {{ action.name }}

    div.top-bar
